- Added `return_ips` query parameter to `GET /api/v1/blocklists` and `GET /api/v1/blocklists/:blocklistId`. Accepted values: `full` (returns full IP objects in `blocklistIps`) and `ip_string` (returns plain IP strings in `blocklistIps`). Replaces the previous `expand_ips` boolean parameter
- Changed `GET /api/v1/blocklists/:blocklistId/ips` to return full IP objects by default. Added `ip_string=true` query parameter to return plain IP strings instead