- Prevent usage of `POST /api/v1/blocklist` and refresh blocklists if API cannot retrieve the list of active decisions
- Prevent from creating decisions of IPs that are on an allowlist
- Improved logging to make the console more clear