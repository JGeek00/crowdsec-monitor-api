- Changed status websocket route to `/api/v1/status`
- Added `GET /api/v1/check-credentials` endpoint
- Prevent running a task (enable, disable, delete) on a blocklist when there's still a different process running for that blocklist