- Added endpoint `POST /api/v1/lists/refresh` that triggers the refresh process for all lists.
- Added endpoint `POST /api/v1/lists/blocklists/:id/refresh` that triggers the refresh process just for that blocklist.