- Now `POST /api/v1/blocklists/check` now returns an array of blocklists instead of one blocklist
- Now `POST /api/v1/blocklists/check-domain` gets the IP address with a DNS resolution instead of with a traceroute