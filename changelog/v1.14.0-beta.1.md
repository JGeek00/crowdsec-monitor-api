- Added endpoint `GET /api/v1/decisions/by-ip` that returns the list of decisions grouped by IP address
- Added endpoint `GET /api/v1/decisions/by-ip/:ip` that returns the IP address details and it's decisions