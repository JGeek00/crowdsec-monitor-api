{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node -r tsconfig-paths/register src/server.ts",
  "events": {
    "restart": "eslint --fix --quiet {{changed}} 2>/dev/null || true"
  }
}
